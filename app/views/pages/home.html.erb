
<div class="card">
  <div class="card-body">
    <h3>Espace, frontière de l'infini, vers laquelle voyage nos vaisseaux.</h3>
    <h3>Explorer de nouveaux mondes étranges, découvrir de nouvelles vies, d'autres civilisations,</h3>
    <h3>et au mépris du danger avancer vers l'inconnu...</h3>
  </div>
</div>

<div class="container mt-5"></div>
  <div class="row">
    <div class="col-6">
      <h3>Our Spaceships for rent</h3>
      <% @spaceships.each do |spaceship| %>
        <% if spaceship.image.attached? %>
          <%= cl_image_tag(spaceship.image.key,
            width: 400, height: 300, crop: :fill)
          %>
        <% end %>
        <h4><%= link_to spaceship.name, spaceship_path(spaceship) %></h4>
      <% end %>
    </div>
    <div class="col-6">
      <%# Mapbox %>
      <div style="width: 100%; height: 600px;"
        data-controller="map"
        data-map-markers-value="<%= @markers.to_json %>"
        data-map-api-key-value="<%= ENV["MAPBOX_API_KEY"] %>">
      </div>
    </div>
  </div>
</div>
